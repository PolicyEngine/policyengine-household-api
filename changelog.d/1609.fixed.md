Modal worker containers now reserve the ~2 CPU cores one calculation uses, cap input concurrency at 2 (autoscale target 1) so simultaneous heavy calculates spread across containers instead of starving on one, and carry a 300s execution budget (gateway 330s) so heavy requests no longer cross the budget cliff where a timeout cancellation shuts down the whole warm container.

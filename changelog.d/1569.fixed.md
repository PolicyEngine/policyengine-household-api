Made retired Modal app cleanup idempotent: stopping an already-deleted app is now treated as success, and cleaned-up apps are pruned from the release manifest so they are not re-listed for cleanup on every release.
